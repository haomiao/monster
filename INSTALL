Thanks for downloading the monster project.

To install it, make sure you have Python 3.4 or greater installed.

Then run this command from the command prompt:

    python setup.py install or python3 setup.py install

    If you're upgrading from a previous version, you need to remove it first.

    For more detailed instructions, see the dir of docs.
